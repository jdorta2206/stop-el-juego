name: iOS App Check

on:
  push:
    branches: [ios-app]
    paths:
      - 'ios-app/**'
      - '.github/workflows/ios-check.yml'
  pull_request:
    branches: [master]
    paths:
      - 'ios-app/**'
      - '.github/workflows/ios-check.yml'

jobs:
  validate:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: ios-app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ios-app/package.json
      - name: Install dependencies
        run: npm install
      - name: Typecheck
        run: npm run typecheck
      - name: Expo doctor
        run: npx expo-doctor
